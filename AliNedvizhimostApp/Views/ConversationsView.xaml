<UserControl x:Class="AliNedvizhimostApp.Views.ConversationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Мои сообщения" FontSize="28" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Content="&lt; Назад" Command="{Binding GoBackCommand}" HorizontalAlignment="Right" Style="{StaticResource SecondaryButton}"/>
        </Grid>

        <ListBox Grid.Row="1" ItemsSource="{Binding Conversations}" Margin="0,20,0,0"
                 SelectedItem="{Binding SelectedConversation}"
                 SelectionMode="Single"
                 HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="{StaticResource Brush.Border.Default}" BorderThickness="0,0,0,1" Padding="10" Margin="0,0,0,5">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock FontWeight="Bold" Text="{Binding PropertyTitle}"/>
                                <TextBlock Text="{Binding PropertyAddress}" Style="{StaticResource Text.Subtle}"/>
                                <TextBlock>
                                    <Run Text="Собеседник: "/>
                                    <Run Text="{Binding OtherUserFirstName}"/>
                                    <Run Text="{Binding OtherUserLastName}"/>
                                </TextBlock>
                                <TextBlock Text="{Binding LastMessageTimestamp, StringFormat='Последнее сообщение: {0:dd.MM.yyyy HH:mm}'}" Style="{StaticResource Text.Subtle}" FontSize="12"/>
                            </StackPanel>
                            <Button Grid.Column="1" Content="Открыть чат" 
                                    Command="{Binding DataContext.OpenConversationCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource PrimaryButton}" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
