<UserControl x:Class="AliNedvizhimostApp.Views.LoginRegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:utils="clr-namespace:AliNedvizhimostApp.Utils"
             mc:Ignorable="d"
             d:DesignHeight="550" d:DesignWidth="800">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Border Style="{StaticResource Container.Card}" MinWidth="450">
            <TabControl SelectionChanged="TabControl_SelectionChanged">
                <TabItem Header="ВХОД">
                    <StackPanel Margin="10">
                        <TextBlock Text="Email:" FontWeight="SemiBold" Margin="0,10,0,5"/>
                        <TextBox Text="{Binding LoginEmail, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="Пароль:" FontWeight="SemiBold" Margin="0,15,0,5"/>
                        <PasswordBox utils:PasswordBoxHelper.BindPassword="True"
                                     utils:PasswordBoxHelper.BoundPassword="{Binding LoginPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource Brush.Destructive}" Margin="0,10,0,0" TextWrapping="Wrap" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding InfoMessage}" Foreground="{StaticResource Brush.Accent.Primary}" Margin="0,5,0,0" TextWrapping="Wrap" HorizontalAlignment="Center"/>
                        
                        <ProgressBar IsIndeterminate="True" Height="10" Margin="0,15,0,0"
                                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                        <Button Content="ВОЙТИ" 
                                Command="{Binding LoginCommand}"
                                Style="{StaticResource PrimaryButton}"
                                Margin="0,20,0,0" HorizontalAlignment="Stretch"/>
                    </StackPanel>
                </TabItem>
                <TabItem Header="РЕГИСТРАЦИЯ">
                    <StackPanel Margin="10">
                        <TextBlock Text="Email:" FontWeight="SemiBold" Margin="0,10,0,5"/>
                        <TextBox Text="{Binding RegisterEmail, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="Пароль:" FontWeight="SemiBold" Margin="0,15,0,5"/>
                        <PasswordBox utils:PasswordBoxHelper.BindPassword="True"
                                     utils:PasswordBoxHelper.BoundPassword="{Binding RegisterPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="Имя:" FontWeight="SemiBold" Margin="0,15,0,5"/>
                        <TextBox Text="{Binding RegisterFirstName, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="Фамилия:" FontWeight="SemiBold" Margin="0,15,0,5"/>
                        <TextBox Text="{Binding RegisterLastName, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource Brush.Destructive}" Margin="0,10,0,0" TextWrapping="Wrap" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding InfoMessage}" Foreground="{StaticResource Brush.Accent.Primary}" Margin="0,5,0,0" TextWrapping="Wrap" HorizontalAlignment="Center"/>
                        
                        <ProgressBar IsIndeterminate="True" Height="10" Margin="0,15,0,0"
                                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                        <Button Content="ЗАРЕГИСТРИРОВАТЬСЯ"
                                Command="{Binding RegisterCommand}"
                                Style="{StaticResource PrimaryButton}"
                                Margin="0,20,0,0" HorizontalAlignment="Stretch"/>
                    </StackPanel>
                </TabItem>
            </TabControl>
        </Border>
    </Grid>
</UserControl>
