<UserControl x:Class="AliNedvizhimostApp.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AliNedvizhimostApp.Views"
             xmlns:viewmodels="clr-namespace:AliNedvizhimostApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="ChatInputTextBoxStyle" TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="VerticalContentAlignment" Value="Top"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="BorderBrush" Value="{StaticResource Brush.Border.Default}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{StaticResource Brush.Background.Content}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource Brush.Border.Focused}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource Brush.Border.Focused}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="{Binding Conversation.PropertyTitle}" FontSize="20" FontWeight="Bold"/>
                <TextBlock Text="{Binding Conversation.PropertyAddress}" FontStyle="Italic" Foreground="Gray"/>
                <TextBlock>
                    <Run Text="Чат с: "/>
                    <Run Text="{Binding Conversation.OtherUserFirstName}"/>
                    <Run Text="{Binding Conversation.OtherUserLastName}"/>
                </TextBlock>
            </StackPanel>
            <Button Grid.Column="1" Content="&lt; Назад" Command="{Binding GoBackCommand}" HorizontalAlignment="Right" Style="{StaticResource SecondaryButton}"/>
        </Grid>

        <ListBox Grid.Row="1" ItemsSource="{Binding Messages}" Margin="0,10,0,10"
                 ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="8" Margin="5" CornerRadius="5"
                            Background="{Binding IsSender, Converter={StaticResource SenderToBackgroundConverter}}">
                        <StackPanel>
                            <TextBlock Text="{Binding SenderName}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Content}" TextWrapping="Wrap"/>
                            <TextBlock Text="{Binding Timestamp, StringFormat='{}{0:dd.MM.yyyy HH:mm}'}" FontSize="10" Foreground="Gray" HorizontalAlignment="Right"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding NewMessageContent, UpdateSourceTrigger=PropertyChanged}"
                     Height="40" Margin="0,0,10,0" Style="{StaticResource ChatInputTextBoxStyle}"/>
            <Button Grid.Column="1" Content="Отправить" Command="{Binding SendNewMessageCommand}" Style="{StaticResource PrimaryButton}"/>
        </Grid>
    </Grid>
</UserControl>
